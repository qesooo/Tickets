@model Tickets.Models.SeguimientoTicket

@{
    ViewData["Title"] = "Eliminar Seguimiento"; // CAMBIO: Título en español

    // Colores consistentes con el proyecto
    var backgroundPastelBlue = "#ADD8E6"; // Fondo de la página
    var deleteButtonColor = "#dc3545";   // Rojo fuerte para el botón de acción "Eliminar"
    var backButtonColor = "#007bff";     // Azul profesional para 'Volver a la lista' (Siguiendo la convención de Detalles/Editar)
}

<style>
    /* 1. ESTILOS GLOBALES Y FONDO */
    body {
        /* Fondo de la página: Celeste Pastel */
        background-color: @backgroundPastelBlue !important;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        min-height: 100vh;
        margin: 0;
        padding: 0;
    }

    /* 2. OCULTAR ELEMENTOS INNECESARIOS (Subtítulo y línea divisoria original) */
    h3, h4, hr {
        display: none;
    }

    /* 3. CONTENEDOR PRINCIPAL Y CENTRADO */
    .page-content-wrapper {
        min-height: 100vh;
        display: flex;
        justify-content: center; /* Centrado horizontal */
        align-items: center; /* Centrado vertical */
        padding: 20px 0;
        width: 100%;
    }

    .details-container {
        /* Tarjeta blanca para los detalles */
        background-color: white;
        padding: 30px 40px;
        border-radius: 10px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        width: 100%;
        max-width: 700px; /* Ancho consistente */
        box-sizing: border-box;
    }

    /* 4. TÍTULOS Y PREGUNTA DE CONFIRMACIÓN */
    h1 {
        /* Título "Eliminar Seguimiento" centrado */
        text-align: center;
        color: #333;
        font-weight: 600;
        margin-bottom: 5px;
        font-size: 2em;
    }

    .confirmation-question {
        text-align: center;
        color: @deleteButtonColor; /* Resaltar la pregunta en rojo */
        font-size: 1.2em;
        font-weight: 500;
        margin-bottom: 30px;
    }

    /* 5. LISTA DE DETALLES (dl) - Estructura de dos columnas */
    .dl-custom {
        display: grid;
        grid-template-columns: 35% 65%; /* Estructura de dos columnas */
        gap: 15px 10px;
        margin-bottom: 30px;
    }

        /* Etiquetas (dt) */
        .dl-custom dt {
            font-weight: 600;
            color: #555;
            text-align: right;
            padding-right: 15px;
            word-wrap: break-word;
        }

        /* Valores (dd) */
        .dl-custom dd {
            font-weight: 400;
            color: #333;
            margin-left: 0;
            word-wrap: break-word;
        }

    /* Estilo para el campo Mensaje (Ocupa el ancho completo) */
    .full-width-field {
        grid-column: 1 / -1;
        padding-top: 15px;
        border-top: 1px solid #eee;
    }

        .full-width-field dt, .full-width-field dd {
            text-align: left;
            padding-right: 0;
            grid-column: 1 / -1;
        }

        .full-width-field dt {
            font-size: 1.1em;
            margin-bottom: 5px;
        }

    /* 6. BOTONES (Consistente) */
    .btn-action {
        width: 100%;
        padding: 12px;
        border: none;
        border-radius: 6px;
        font-size: 1.1em;
        font-weight: 600;
        cursor: pointer;
        transition: background-color 0.2s;
        margin-top: 10px;
        text-decoration: none;
        display: block;
        text-align: center;
        color: white;
    }

    /* Botón "Eliminar" (Rojo fuerte) */
    .btn-delete {
        background-color: @deleteButtonColor;
        margin-top: 20px;
    }

        .btn-delete:hover {
            background-color: #c82333;
        }

    /* Botón "Volver a la lista" (Azul profesional) */
    .btn-volver {
        background-color: @backButtonColor;
        margin-top: 15px;
    }

        .btn-volver:hover {
            background-color: #0056b3;
        }
</style>

<div class="page-content-wrapper">
    <div class="details-container">

        <h1>@ViewData["Title"]</h1>

        <p class="confirmation-question">¿Está seguro que desea eliminar este seguimiento?</p>

        <dl class="dl-custom">
            <dt>
                Ticket
            </dt>
            <dd>
                @Html.DisplayFor(model => model.IdTicketNavigation.IdTicket)
            </dd>

            <dt>
                Usuario
            </dt>
            <dd>
                @Html.DisplayFor(model => model.IdUsuarioNavigation.IdUsuario)
            </dd>

            <dt>
                Fecha de Seguimiento
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Fecha)
            </dd>

            <div class="full-width-field">
                <dt>
                    @Html.DisplayNameFor(model => model.Mensaje)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Mensaje)
                </dd>
            </div>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="IdSeguimiento" />

            <input type="submit" value="Eliminar" class="btn-action btn-delete" />

            <a asp-action="Index" class="btn-action btn-volver">Volver a la lista</a>
        </form>
    </div>
</div>